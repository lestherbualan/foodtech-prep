import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette — professional teal
  static const Color primary = Color(0xFF0D7377);
  static const Color primaryLight = Color(0xFF14A3A8);
  static const Color primaryDark = Color(0xFF095456);

  // Secondary palette
  static const Color secondary = Color(0xFF32936F);
  static const Color secondaryLight = Color(0xFF48B890);

  // Surface & background
  static const Color surface = Color(0xFFF5F7F8);
  static const Color background = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF1A2E35);
  static const Color textSecondary = Color(0xFF5A7A84);
  static const Color textHint = Color(0xFF9CB0B8);

  // Status
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFF57C00);

  // Other
  static const Color divider = Color(0xFFE0E8EB);
  static const Color disabled = Color(0xFFB0BEC5);
}
